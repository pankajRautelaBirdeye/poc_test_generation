name: Auto Test Generation (OpenAI)

on:
  push:
    branches: [ test_generation_v2 ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build project
        run: mvn -q -DskipTests package

      - name: Build classpath
        run: mvn -q -DincludeScope=runtime dependency:build-classpath -Dmdep.outputFile=cp.txt

      - name: Scan project for annotated APIs
        run: |
          CP=$(cat cp.txt):target/classes
          java -cp "$CP" com.example.poc_test_generation.tools.ScanAnnotations
          cat methods.json

      - name: Generate Tests using OpenAI gpt-4o-mini
        run: |
          echo "[" > combined_output.json
          jq -c '.[]' methods.json | while read method; do
            echo "📌 Generating test for method..."
            REQUEST=$(jq -n \
              --arg method "$method" \
              '{
                model: "gpt-4o-mini",
                input: [
                  {
                    "role": "system",
                    "content": "You generate ONLY clean JUnit5 test classes for Spring Boot @WebMvcTest controllers. Use @MockBean for dependencies, autowire ObjectMapper, include all imports, place class in package com.example.demo.generated. Output exactly one ```java code block."
                  },
                  {
                    "role": "user",
                    "content": ("Generate a complete production-grade Spring Boot JUnit test for the following:\n\n" + $method)
                  }
                ],
                max_output_tokens: 4096,
                temperature: 0.2
              }')
            RESPONSE=$(curl -s https://api.openai.com/v1/responses \
              -H "Authorization: Bearer ${{ secrets.OPENAI_API_KEY }}" \
              -H "Content-Type: application/json" \
              -d "$REQUEST")
            echo "$RESPONSE," >> combined_output.json
          done
          sed -i '$ s/,$//' combined_output.json
          echo "]" >> combined_output.json

      - name: Write generated test files
        run: |
          CP=$(cat cp.txt):target/classes
          java -cp "$CP" com.example.poc_test_generation.tools.WriteGeneratedTests

      - name: Compile Generated Tests
        run: mvn clean compile test-compile

      - name: Commit & Push Generated Tests
        run: |
          git config user.email "ai-test-bot@example.com"
          git config user.name "AI Test Bot"
          git add src/test/java/com/example/demo/generated || true
          git commit -m "✅ Auto-generated tests using OpenAI gpt-4o-mini" || true
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} HEAD:main
